@baseUrl = http://127.0.0.1:8080
@adminUsername = admin
@employeeUsername = chen_xu
@password = 123456

### 正常路径：管理员登录
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "{{adminUsername}}",
  "password": "{{password}}"
}

### 正常路径：获取项目模板列表，默认只返回启用模板
GET {{baseUrl}}/api/project-templates

### 正常路径：按状态获取停用模板列表
GET {{baseUrl}}/api/project-templates?status=2

### 正常路径：管理员登出
POST {{baseUrl}}/api/auth/logout
Content-Type: application/json

{}

### 异常路径：未登录获取项目模板列表，预期 401
GET {{baseUrl}}/api/project-templates

### 正常路径：普通员工登录
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "{{employeeUsername}}",
  "password": "{{password}}"
}

### 正常路径：普通员工获取项目模板列表
GET {{baseUrl}}/api/project-templates

### 正常路径：普通员工登出
POST {{baseUrl}}/api/auth/logout
Content-Type: application/json

{}

### 异常路径：重新登录管理员
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "{{adminUsername}}",
  "password": "{{password}}"
}

### 异常路径：status 不是整数，预期 400
GET {{baseUrl}}/api/project-templates?status=enabled

### 异常路径：status 不在枚举范围内，预期 400
GET {{baseUrl}}/api/project-templates?status=3
