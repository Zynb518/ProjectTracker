@baseUrl = http://127.0.0.1:8080
@adminUsername = admin
@memberUsername = chen_xu
@nonMemberUsername = wang_jie
@password = 123456
@projectId = 1
@invalidProjectId = 0
@missingProjectId = 999999

### 提示：当前种子数据中，project 1（官网重构）下已有阶段节点，项目成员 chen_xu 可见，wang_jie 不属于该项目。
### 当前为 12.1 获取项目阶段节点列表 的先行失败用例；接口未实现前，相关请求会先失败，后续据此补实现。

### 正常路径：管理员登录
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "{{adminUsername}}",
  "password": "{{password}}"
}

### 正常路径：管理员获取项目阶段节点列表
GET {{baseUrl}}/api/projects/{{projectId}}/nodes

### 正常路径：管理员登出
POST {{baseUrl}}/api/auth/logout
Content-Type: application/json

{}

### 异常路径：未登录获取项目阶段节点列表，预期 401
GET {{baseUrl}}/api/projects/{{projectId}}/nodes

### 正常路径：项目成员登录
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "{{memberUsername}}",
  "password": "{{password}}"
}

### 正常路径：项目成员获取自己参与项目的阶段节点列表
GET {{baseUrl}}/api/projects/{{projectId}}/nodes

### 正常路径：项目成员登出
POST {{baseUrl}}/api/auth/logout
Content-Type: application/json

{}

### 正常路径：非项目成员登录
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "{{nonMemberUsername}}",
  "password": "{{password}}"
}

### 异常路径：非项目成员获取阶段节点列表，预期 403
GET {{baseUrl}}/api/projects/{{projectId}}/nodes

### 正常路径：非项目成员登出
POST {{baseUrl}}/api/auth/logout
Content-Type: application/json

{}

### 异常路径：重新登录管理员
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "{{adminUsername}}",
  "password": "{{password}}"
}

### 异常路径：project_id 小于等于 0，预期 400
GET {{baseUrl}}/api/projects/{{invalidProjectId}}/nodes

### 异常路径：项目不存在，预期 404
GET {{baseUrl}}/api/projects/{{missingProjectId}}/nodes
