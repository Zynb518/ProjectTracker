@baseUrl = http://127.0.0.1:8080
@adminUsername = admin
@memberUsername = chen_xu
@nonMemberUsername = wang_jie
@password = 123456
@subTaskId = 1
@invalidSubTaskId = 0
@missingSubTaskId = 999999

### 提示：当前种子数据中，subtask 1 为 project 1（官网重构）下“需求分析”节点的子任务“页面清单整理”。
### 提示：chen_xu 是该项目成员且也是该子任务负责人，wang_jie 不属于该项目，可直接回放权限相关场景。
### 当前为 13.4 获取子任务详情 的联调用例；接口主链路已实现后补到这里，按顺序回放即可。

### 正常路径：管理员登录
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "{{adminUsername}}",
  "password": "{{password}}"
}

### 正常路径：管理员获取子任务详情
GET {{baseUrl}}/api/subtasks/{{subTaskId}}

### 正常路径：管理员登出
POST {{baseUrl}}/api/auth/logout
Content-Type: application/json

{}

### 异常路径：未登录获取子任务详情，预期 401
GET {{baseUrl}}/api/subtasks/{{subTaskId}}

### 正常路径：项目成员登录
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "{{memberUsername}}",
  "password": "{{password}}"
}

### 正常路径：项目成员获取自己参与项目中的子任务详情
GET {{baseUrl}}/api/subtasks/{{subTaskId}}

### 正常路径：项目成员登出
POST {{baseUrl}}/api/auth/logout
Content-Type: application/json

{}

### 正常路径：非项目成员登录
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "{{nonMemberUsername}}",
  "password": "{{password}}"
}

### 异常路径：非项目成员获取子任务详情，预期 403
GET {{baseUrl}}/api/subtasks/{{subTaskId}}

### 正常路径：非项目成员登出
POST {{baseUrl}}/api/auth/logout
Content-Type: application/json

{}

### 异常路径：重新登录管理员
POST {{baseUrl}}/api/auth/login
Content-Type: application/json

{
  "username": "{{adminUsername}}",
  "password": "{{password}}"
}

### 异常路径：subtask_id 小于等于 0，预期 400
GET {{baseUrl}}/api/subtasks/{{invalidSubTaskId}}

### 异常路径：子任务不存在，预期 404
GET {{baseUrl}}/api/subtasks/{{missingSubTaskId}}
